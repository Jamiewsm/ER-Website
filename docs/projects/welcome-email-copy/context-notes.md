# 작업 맥락

- 2026-09-15. `site/welcome-email-copy`, 시작 HEAD `aa10703`. 별도 Supabase 작업은 이 브랜치에 포함하지 않는다.
- 실제 사이트는 `js/sections/apply.js`, `js/coach/applications.js`, `js/coach/views.js`를 사용한다. 구 monolithic main.js 또는 진단 테스트 런타임은 수정하지 않는다.
- 추가로 승인된 범위는 `index.html`의 위 세 스크립트 참조 버전을 `20260915a`로 갱신하는 작업이다. 다른 스크립트와 문서 구조는 유지한다.
- 등록 준비는 결제 대기/정원 예약을 위한 기존 서버 이벤트다. 발송을 생략해도 이 작업은 수행되므로 UI에서 실패나 새 발송으로 오인하지 않게 한다.
- 신청 성공 응답은 이메일 공급자 발송 성공을 항상 보장하지 않는다. 완료 화면은 이메일을 확인하도록 안내하며 검증하지 않은 발송 완료·24시간 약속을 단정하지 않는다.
- 등록 확정 상태 저장 후 사용자가 선택한 후속 안내를 보내는 순서를 유지한다. 새 백엔드에서 10월 기본과정만 지원하는 안내를 과거 기수에 잘못 요청하지 않는다.
- `email_delivery_uncertain`, `email_sent_record_failed`, `email_delivery_claim_failed` 등 서버 오류는 한국어 message를 전달한다. 메일 ID가 있을 때만 발송 완료라고 표시하며 `already_sent`는 등록 준비 완료와 재발송 생략으로 표현한다.
- 과거 고정 정원 8명 안내를 없애고 실제 기수 정원이 찼다는 서버 결과만 전달한다. 기존 정원 RPC 및 이벤트명은 바꾸지 않았다.
- 사전 설문을 사용하는 별도의 Typing 상담 문구는 이 변경과 관계없으므로 유지했다. 심화 신청 폼이나 기수는 만들지 않았다.
- 배포 의존성. 통합 접수·결제 및 강의계획안 본문을 지원하는 연결된 Supabase 변경을 먼저 배포해야 한다. 현재 사이트 커밋만으로 백엔드 메일 동작이 바뀌지는 않는다.
- 검증. 신청 렌더와 기존 관리자 가상 요청 focused 25개 통과. CI의 기본 Node 회귀 항목 및 추가 site 검사를 합친 146개 통과. 세 JS 문법 검사와 git diff --check 통과. 실제 메일을 발송하거나 운영 상태를 변경하지 않았다.
