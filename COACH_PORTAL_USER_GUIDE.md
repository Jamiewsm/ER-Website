# ER 코치 포털 웹앱 상세 사용 가이드

처음 사용하는 코치를 위한 모바일 웹앱 기준 안내서입니다.  
이 가이드는 현재 `Coach Portal Web App` 화면 구조를 기준으로 작성되었습니다.

## 1. 이 웹앱으로 할 수 있는 일

ER 코치 포털에서는 아래 업무를 스마트폰에서 한 번에 처리할 수 있습니다.

- 홈 화면에서 이번 주 우선순위 확인
- 주간트랙 진행 현황 확인
- 실습트랙 등록 및 Typing 보고서 제출
- 세션 캘린더에서 일정 생성, 수정, 삭제
- 멘토링 주간 보고서 작성
- 자료 업로드, 다운로드, 수정
- 게시물별 댓글 확인 및 작성
- 프로필 사진 업로드와 로그아웃

## 2. 접속 전 준비

사용 전 아래 조건이 충족되어야 합니다.

- 코치 계정이 `coach_profiles`에 등록되어 있어야 합니다.
- 계정이 `is_active = true` 상태여야 합니다.
- 코치용 로그인 이메일과 비밀번호를 알고 있어야 합니다.

권한이 없으면 로그인은 되어도 포털 진입이 제한됩니다.

## 3. 접속 주소와 권장 사용 환경

- 권장 주소: `https://coach.er-coaching.com/`
- 권장 기기: 스마트폰 세로 모드
- 권장 방식: 브라우저에서 열기보다 `홈 화면에 추가` 후 앱처럼 사용

이 앱은 모바일 우선 PWA입니다. 아이폰 Safari, 안드로이드 Chrome/삼성 인터넷에서 가장 안정적으로 동작합니다.

## 4. 첫 로그인 방법

로그인 화면에서는 3가지 진입 방법이 있습니다.

1. 이메일 로그인
- 이메일 입력
- 비밀번호 입력
- `로그인` 버튼 선택

2. Google 로그인
- `Google 로그인` 버튼 선택
- 코치 권한이 연결된 Google 계정으로 로그인

3. 비밀번호 재설정
- 비밀번호를 모르면 `비밀번호 재설정 이메일 받기` 버튼 선택
- 등록된 이메일 주소로 재설정 메일 수신

로그인 후 코치 권한이 확인되면 자동으로 포털 메인 화면으로 이동합니다.

## 5. 처음 로그인했을 때 먼저 할 일

처음 로그인하면 아래 순서로 확인하는 것을 권장합니다.

1. 홈 화면 상단 인사 영역 확인
- 본인 이름/역할
- 이번 주 핵심 작업
- 빠른 이동 버튼 `주간트랙 보기`, `실습트랙 보기`

2. 설치 배너 확인
- 가능하면 `설치 안내` 또는 `설치` 버튼으로 홈 화면에 추가
- 앱처럼 열면 주소창 간섭이 줄고 사용성이 좋아집니다

3. 하단 메뉴 구조 익히기
- `홈`
- `주간트랙`
- `실습트랙`
- `일정`
- `자료`

4. 오른쪽 상단 프로필 메뉴 확인
- 프로필 사진 업로드
- 로그아웃

## 6. 홈 화면 보는 법

홈 화면은 코치가 가장 자주 보는 요약 대시보드입니다.

### 6-1. 우선순위

가장 먼저 처리해야 할 항목이 카드 형태로 보입니다.  
예를 들어 아래 정보가 표시될 수 있습니다.

- 미완료 보고서 수
- 이번 주 보고서 수
- 다른 코치들의 최근 보고서

카드를 누르면 관련 화면으로 바로 이동합니다.

### 6-2. 최근 제출 보고서

내가 최근 제출한 보고서를 빠르게 다시 열 수 있습니다.

- 멘토링 주간 보고서
- 트랙별 보고서
- 실습 관련 보고서

보고서를 열면 상세 내용과 댓글을 볼 수 있습니다.

### 6-3. 상단 빠른 이동 버튼

- `주간트랙 보기`: 트랙 현황 화면으로 이동
- `실습트랙 보기`: 실습 관리 화면으로 이동

## 7. 하단 메뉴별 사용법

## 7-1. 홈

사용 목적:

- 오늘/이번 주 해야 할 일 확인
- 최근 제출 보고서 재확인
- 다른 메뉴로 빠르게 이동

추천 상황:

- 출근 직후 오늘 할 일 확인
- 보고서 제출 누락 여부 확인
- 다른 코치 피드백이나 최근 활동 점검

## 7-2. 주간트랙

이 화면에서는 코치 훈련 흐름을 트랙별로 확인합니다.

주요 트랙 예시:

- Formation 트랙
- 강사트랙
- 코칭스킬 트랙
- 실습트랙

확인 가능한 내용:

- 각 트랙의 진행 현황
- 최근 제출 보고서 제목
- 내 보고서 목록
- 다른 코치 보고서 보기

보고서 작성 흐름:

1. 해당 트랙 카드 또는 버튼 선택
2. `보고서 작성` 또는 기존 보고서 열기
3. 질문형 입력 폼 작성
4. `제출` 또는 `수정 제출`

참고:

- 강사트랙과 코칭스킬 트랙은 질문형 보고서 양식이 내장되어 있습니다.
- 주차 라벨은 자동 제안값이 들어갈 수 있지만 필요하면 수정 가능합니다.

## 7-3. 실습트랙

이 화면은 코치별 실습을 관리하는 곳입니다.

### 실습트랙에서 할 수 있는 일

- 새 실습 모듈 추가
- 기존 실습 수정
- 비활성화
- 모듈 삭제
- 실습별 보고서 확인
- Typing 보고서 제출
- 멘토링 주간 보고서 작성

### 새 실습 추가 방법

1. `실습트랙` 탭 이동
2. 상단 `실습 추가` 버튼 선택
3. 아래 항목 입력
- 실습 유형
- 실습 제목
- 설명
- 주기
- 시작일/종료일
4. `추가` 선택

실습 유형 예시:

- 양성반 멘토링
- 스터디 인도
- Typing 세션
- 코티칭
- 참관
- 단기 실습(기타)

### Typing 세션 실습 주의사항

`Typing 세션`을 선택하면 일부 값이 자동으로 고정됩니다.

- 제목: `Typing Session Practicum`
- 설명: 월 1회 외부인 대상 Typing 실습 설명
- 주기: `monthly`

즉, Typing은 월간 실습 기준으로 관리됩니다.

### Typing 보고서 제출 방법

1. `실습트랙` 탭 이동
2. Typing 실습 카드에서 `이번 달 보고서 제출` 또는 기존 보고서 열기
3. 아래 항목 입력
- 코치이름
- 내담자 유형
- 세션 일시
- 녹화/녹음 링크
- 질문형 서술 문항
4. `제출` 선택

추가 규칙:

- 월 1회 제출 기준입니다.
- 일부 문항은 비워 두어도 제출 가능합니다.
- 수석 코치는 리뷰 상태까지 변경할 수 있습니다.

## 7-4. 일정

이 화면은 세션 캘린더입니다.

### 할 수 있는 일

- 월별 일정 보기
- 날짜별 일정 확인
- 일정 추가
- 일정 수정
- 일정 삭제
- 일정 상세에서 링크 열기
- 일정별 댓글 작성

### 새 일정 추가 방법

1. `일정` 탭 이동
2. 상단 `일정 추가` 버튼 선택
3. 아래 항목 입력
- 제목
- 유형
- 하루종일 여부
- 시작/종료 일시 또는 시작/종료일
- 위치/링크
- 메모
4. `생성` 선택

### 일정 유형

- 멘토링
- 강사트랙
- Formation 트랙
- 코칭스킬 트랙
- 실습트랙
- ER 사역
- 기타

### 멘토링 일정 등록 시 주의

일정 유형을 `멘토링`으로 선택하면 멘티 선택 항목이 추가됩니다.

- 멘티 선택은 필수입니다.
- 제목은 자동으로 `멘토링 · 멘티이름` 형식으로 맞춰집니다.
- 멘티 연결 정보가 메모 내부에 함께 저장됩니다.

### 일정 상세 화면에서 가능한 작업

- 시간 확인
- 위치/링크 확인
- `모임 Join` 버튼으로 외부 링크 열기
- 메모 읽기
- 댓글 작성/답글/수정/삭제
- 작성자 본인 또는 수석 코치는 수정/삭제 가능

## 7-5. 자료

이 화면은 자료실입니다. 업로드 자료와 일부 보고서가 폴더형으로 정리되어 보일 수 있습니다.

### 할 수 있는 일

- 자료 업로드
- 자료 수정
- 자료 다운로드
- 폴더별 정리 확인
- 보고서 폴더 열람

### 자료 업로드 방법

1. `자료` 탭 이동
2. 상단 `자료 업로드` 버튼 선택
3. 아래 항목 입력
- 제목
- 설명
- 폴더명
- 파일
4. `업로드` 선택

### 자료 수정 방법

기존 자료를 열면 제목, 설명, 폴더명 수정이 가능하며 파일도 교체할 수 있습니다.

## 8. 멘토링 주간 보고서 작성 방법

이 앱의 핵심 업무 중 하나입니다. 보통 홈, 실습트랙, 관련 카드에서 진입합니다.

작성 흐름:

1. 멘티 또는 주간 보고서 관련 버튼 선택
2. `주간 보고서 작성` 또는 `보고서 수정` 선택
3. 아래 순서대로 입력
- A. 멘티 상태 체크
- B. 멘티 응답 기록
- C. 멘토 실습 성찰
4. `제출` 선택

필수 규칙:

- `멘티 상태 체크`는 반드시 선택해야 합니다.
- 제출 후 관련 주간 보고서 업무 카드가 자동 갱신됩니다.

보고서 삭제:

- 기존 보고서를 열면 삭제 버튼이 표시될 수 있습니다.
- 삭제 시 연결된 주간 보고서 업무도 함께 정리됩니다.

## 9. 댓글과 알림 사용법

일정, 보고서 등 상세 화면에서 댓글 기능을 사용할 수 있습니다.

가능한 작업:

- 댓글 작성
- 답글 작성
- 본인 댓글 수정
- 본인 댓글 삭제
- 수석 코치는 전체 관리 가능

오른쪽 상단 종 아이콘에서는 댓글 알림 수를 확인할 수 있습니다.

## 10. 프로필 메뉴 사용법

오른쪽 상단 프로필 이미지를 누르면 메뉴가 열립니다.

가능한 작업:

- `사진 업로드`
- `로그아웃`

프로필 사진은 코치 프로필과 댓글/화면 표시 영역에 반영될 수 있습니다.

## 11. 홈 화면에 추가하는 방법

### iPhone Safari

1. 하단 공유 버튼 선택
2. `홈 화면에 추가` 선택
3. 이름 확인 후 추가

### Android

1. 브라우저 메뉴 열기
2. `설치` 또는 `홈 화면에 추가` 선택
3. 홈 화면 아이콘으로 실행

설치 후에는 일반 웹페이지보다 앱처럼 더 안정적으로 사용할 수 있습니다.

## 12. 처음 쓰는 코치를 위한 추천 사용 순서

처음 1주차에는 아래 순서로 익히면 됩니다.

1. 로그인 후 홈 화면 구조 익히기
2. 홈 화면에 추가해서 앱처럼 실행하기
3. `주간트랙`에서 내 트랙과 보고서 구조 확인
4. `실습트랙`에서 담당 실습 확인 또는 실습 추가
5. `일정`에서 이번 주 세션 일정 확인
6. 첫 멘토링 주간 보고서 작성
7. `자료` 탭에서 필요한 자료 다운로드

## 13. 자주 발생하는 문제와 해결 방법

### 1. 로그인은 되는데 포털이 안 열립니다

가능한 원인:

- 코치 계정이 아님
- `coach_profiles`에 등록되지 않음
- `is_active = false`

확인 방법:

- 운영자에게 계정 활성 상태 확인 요청

### 2. 비밀번호가 맞지 않습니다

해결:

- 로그인 화면에서 `비밀번호 재설정 이메일 받기` 사용

### 3. Google 로그인이 안 됩니다

가능한 원인:

- 코치 권한이 없는 Google 계정 사용
- 브라우저 세션 충돌

해결:

- 다른 계정 로그아웃 후 다시 시도
- 이메일 로그인으로 우선 접속

### 4. 자료 업로드가 실패합니다

가능한 원인:

- 파일 용량 또는 네트워크 문제
- Storage 정책/버킷 설정 문제

해결:

- 네트워크 재확인 후 재시도
- 운영자에게 스토리지 설정 확인 요청

### 5. 일정 링크가 열리지 않습니다

가능한 원인:

- 링크 형식이 잘못 입력됨
- 브라우저 팝업/외부 링크 제한

해결:

- `https://` 포함 여부 확인
- 링크를 수정 후 다시 저장

### 6. 보고서 수정 버튼이 안 보입니다

가능한 원인:

- 본인이 작성한 보고서가 아님

정책:

- 작성자 본인만 수정 가능
- 수석 코치는 예외적으로 관리 가능

## 14. 권한 정책 요약

- 일반 코치: 본인 데이터 중심으로 작성/수정
- 수석 코치(`head_coach`): 더 넓은 관리 권한 보유

수석 코치가 일반적으로 더 많이 할 수 있는 작업:

- 다른 코치 작성물 관리
- Typing 리뷰 상태 조정
- 일부 전체 관리 기능 접근

## 15. 운영팀용 체크리스트

신규 코치 온보딩 시 아래 항목을 함께 확인하면 좋습니다.

- 코치 계정 생성 완료
- 활성화 상태 확인
- 첫 로그인 성공 확인
- 홈 화면 추가 안내 완료
- 이번 주 보고서 제출 경로 설명 완료
- 멘토링 일정 1건 이상 연결 확인
- 자료 다운로드 테스트 완료

## 16. 한 줄 요약

처음 사용하는 코치는 아래 4가지만 먼저 익히면 됩니다.

- `홈`에서 이번 주 할 일 확인
- `주간트랙`에서 보고서 작성
- `실습트랙`에서 Typing/실습 관리
- `일정`과 `자료`에서 세션 운영 지원
