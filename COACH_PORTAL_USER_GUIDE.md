# Coach Portal 사용 가이드

## 1) 인터페이스 확인 방법

네, 웹사이트에 들어가면 보입니다.
다만 **코치 권한 계정으로 로그인했을 때만** `Coach Portal` 화면으로 진입됩니다.

확인 순서:
1. 사이트 접속: `https://er-coaching.com` (또는 현재 배포 주소)
2. 우측 상단 사용자 아이콘 클릭
3. 코치 계정으로 로그인
4. 아래 중 하나로 진입
   - 로그인 직후 자동으로 `Coach Portal` 진입
   - `My Page` 안의 `Coach Portal` 버튼 클릭

코치 권한이 없으면:
- `My Page`만 보이거나
- 코치 화면 접근 시 "접근 권한이 없습니다" 화면이 보입니다.

---

## 2) 사용 전 필수 준비

아래 2가지는 먼저 완료되어야 합니다.

1. Supabase SQL 스키마 실행
- 파일: `supabase/coach_portal_schema.sql`

2. 코치 계정 등록
- 파일: `supabase/coach_profiles_seed.sql`
- 코치 이메일을 실제 이메일로 바꾼 뒤 실행

관리자 계정 예시:
- `campus.12000@gmail.com` -> `head_coach` 권한 지정

---

## 3) 화면 구성

`Coach Portal`에는 3개 주요 메뉴가 있습니다.

1. `과제 관리`
- 과제 등록
- 과제 목록 확인
- 첨부파일 업로드

2. `코치 자료실`
- 자료 업로드
- 자료 목록 확인
- 다운로드

3. `주간 일정`
- 스터디/트레이닝 일정 등록
- 다가오는 일정 목록 확인

---

## 4) 기능별 사용법

## 4-1) 과제 등록

1. `Coach Portal` -> `과제 관리`
2. `새 과제 등록` 폼 입력
   - 과제 제목 (필수)
   - 마감일(선택)
   - 주차 라벨(예: `2026-W10`)
   - 상태 (`게시`, `임시저장`, `보관`)
   - 설명
   - 첨부파일(복수 가능)
3. `과제 저장` 클릭
4. 아래 과제 목록에 반영 확인

## 4-2) 자료 업로드 / 다운로드

1. `Coach Portal` -> `코치 자료실`
2. `자료 업로드` 폼 입력
   - 자료 제목 (필수)
   - 카테고리 (일반/스터디/트레이닝/참고자료)
   - 설명
   - 파일 1개 선택
3. `자료 업로드` 클릭
4. 목록에서 `다운로드` 버튼으로 파일 확인

## 4-3) 주간 일정 등록

1. `Coach Portal` -> `주간 일정`
2. `일정 등록` 폼 입력
   - 일정 제목 (필수)
   - 타입 (`스터디` / `트레이닝`)
   - 시작/종료 시각 (필수)
   - 장소/링크
   - 메모
3. `일정 저장` 클릭
4. 아래 `다가오는 일정`에서 확인

---

## 5) 권한 정책 요약

1. `is_active=true` 코치만 포털 접근 가능
2. 코치는 과제/자료/일정 열람 및 등록 가능
3. `head_coach`는 코치 프로필(권한) 관리 가능

---

## 6) 자주 발생하는 문제

1. 로그인했는데 코치 포털이 안 보임
- 해당 이메일이 `public.coach_profiles`에 없거나 `is_active=false`

2. 업로드 실패
- Storage 버킷/정책 미적용 가능성
- `coach_portal_schema.sql` 재실행 후 확인

3. 다운로드 실패
- Storage 정책 또는 signed URL 생성 권한 이슈
- 로그인 계정이 코치 권한인지 확인

---

## 7) 운영 체크리스트

주 1회 점검 권장:
1. 코치 계정 활성화 상태 (`is_active`) 확인
2. 최근 업로드 파일 정상 다운로드 확인
3. 일정 등록/조회 동작 확인
4. 신규 코치 추가 시 `coach_profiles` 즉시 반영
